# Cloudflare Pages — https://developers.cloudflare.com/pages/
# Git deploy: connect repo in dashboard; these settings apply to output dir.
name = "edgequity"
compatibility_date = "2025-05-01"
pages_build_output_dir = "dist"
