services:
  - type: web
    name: valuwise-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn financials:app
    envVars:
      - key: EDGAR_IDENTITY
        value: valuwise-app admin@valuwise.app
      - key: PYTHON_VERSION
        value: "3.12"
